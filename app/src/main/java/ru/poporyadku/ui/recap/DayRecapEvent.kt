package ru.poporyadku.ui.recap

/**
 * События экрана итога дня.
 *
 * Единственное действие сегодняшнего итога — «Готово»: «Поделиться» появится в
 * итерации 5, реклама и запрос уведомлений — в итерациях 6 и 7, и точки для них здесь
 * не резервируются (пустой хук — тот же мёртвый код).
 */
sealed interface DayRecapEvent {
    data object DoneClicked : DayRecapEvent
}
