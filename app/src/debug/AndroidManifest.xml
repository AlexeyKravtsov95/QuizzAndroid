<?xml version="1.0" encoding="utf-8"?>
<!-- ITERATION_2_DESIGN.md, раздел 6 / D-10: manifest merger добавляет Activity только
     в debug-вариант. Ноль строк в src/main — ни маршрута, ни константы, ни заглушки. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity
            android:name="ru.poporyadku.debug.DebugActivity"
            android:exported="true"
            android:label="@string/debug_activity_label"
            android:theme="@style/Theme.PoPoRyadku">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
